describe('Demo test', () => {
    it('suma básica', () => {
        expect(2 + 2).toBe(4);
    });

    it('resta básica', () => {
        expect(2 - 2).toBe(0);
    });
});

// da lo mismo a testear se dijo